{
  "name": "@emr-webmcp/simulation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./profile-schema": "./src/profile-schema.ts",
    "./manifest": "./src/manifest.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "generate": "node --experimental-strip-types ./src/manifest.ts"
  },
  "dependencies": {
    "zod": "4.5.4"
  }
}
