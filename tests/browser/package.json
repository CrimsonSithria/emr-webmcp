{
  "name": "@emr-webmcp/browser-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint fixtures harness specs scenarios playwright.config.ts vite.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "playwright test"
  },
  "devDependencies": {
    "@emr-webmcp/core": "workspace:*",
    "@playwright/test": "1.55.1",
    "typescript": "5.9.3",
    "vite": "7.1.5"
  }
}
