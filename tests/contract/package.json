{
  "name": "@emr-webmcp/contract-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@emr-webmcp/contract-fixture": "workspace:*",
    "@emr-webmcp/core": "workspace:*"
  }
}
