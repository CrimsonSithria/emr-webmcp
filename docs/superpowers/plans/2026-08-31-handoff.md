# EMR WebMCP Implementation Handoff

## Repository state

- Repository: `https://github.com/CrimsonSithria/emr-webmcp`
- Visibility: private
- Clean base: `main` at `92ed2cd`
- Design: `docs/superpowers/specs/2026-08-31-emr-webmcp-design.md`
- Master sequence: `docs/superpowers/plans/2026-08-31-emr-webmcp-master.md`
- Phase plans: foundation, OpenMRS adapter/O3, workflows/review UI, simulation/evaluation, and Crimson Prime deployment in this directory.
- Crimson plan: `d4cec05b` (`Phase 1 — EMR WebMCP Foundation and Contracts`)
- Current queue: bootstrap task `48bd432e` failed; the other three Phase 1 tasks remain dependency-blocked and queued.
- No Crimson workers are running. `main` contains documentation only.

## What happened in the first Phase 1 run

The bootstrap worker produced commit `dcd978e` on branch `swarm/crimson-48bd432e/work`. The autonomous reviewer passed its scope, pinned dependencies, immutable install, typecheck, and lint.

Integration then failed before merge because Agent Forest auto-detected a root `package.json` without a lockfile on the parent branch and ran `npm install`. The child branch is a Yarn 4 workspace; npm converted workspace references to `file:` entries, after which `yarn install --immutable` correctly rejected a lockfile change. The implementation itself was not rejected.

A remediation run was started with its local `setup_command` changed to:

```bash
npx -y corepack@0.36.0 yarn install --immutable
```

The user requested that all agents stop before that remediation completed. Do not weaken or remove the immutable lockfile gate.

## Recommended continuation

1. Confirm no other agent owns this repository and that `.git/index.lock` is absent.
2. Inspect `dcd978e` against the foundation plan; it is the already-reviewed bootstrap implementation.
3. Resume through Crimson with an explicit Yarn setup command on every Node task. If the planner cannot persist `setup_command`, use the failed plan's repair-station path or submit a replacement contract that does.
4. Do not run plain `npm install` in this repository.
5. Complete Phase 1 and run its full verification block before starting the OpenMRS adapter phase.
6. Keep upstream OpenMRS unmodified and preserve the human-confirmed durable-write boundary.

Useful inspection commands:

```bash
crimson plan-show d4cec05b
git show --stat --oneline dcd978e
git diff 92ed2cd..dcd978e
git status --short --branch
```

The local factory directories `.agentforest/`, `.spine/`, and `AGENTS.md` are excluded and must never be committed upstream.
